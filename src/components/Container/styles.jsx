import { styled } from '@stitches/react';

export const ContainerStyles = styled('div', {
  width: '100%',
  minHeight: '100vh',
  maxWidth: 1040,
  textAlign: 'center',
  padding: '$64 $20',
  margin: '0 auto',
});
