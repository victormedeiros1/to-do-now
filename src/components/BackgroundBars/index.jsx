import { BackgroundBarsStyles, BottomBar, TopBar } from './styles';

const BackgroundBars = () => {
  return (
    <BackgroundBarsStyles>
      <TopBar />
      <BottomBar />
    </BackgroundBarsStyles>
  );
};

export default BackgroundBars;
