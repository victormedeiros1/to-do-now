import { BackgroundStyles, Bar } from './styles';

const Background = () => {
  return (
    <BackgroundStyles>
      <Bar position="top" />
      <Bar position="bottom" />
    </BackgroundStyles>
  );
};

export default Background;
