import { BackgroundStyles, BottomBar, TopBar } from './styles';

const Background = () => {
  return (
    <BackgroundStyles>
      <TopBar />
      <BottomBar />
    </BackgroundStyles>
  );
};

export default Background;
