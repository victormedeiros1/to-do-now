import { createTheme } from '/stitches.config';

export const lightTheme = createTheme('light-theme', {
  colors: {
    light: '#fff',
    dark: '#222',
    lightgray: '#999',
    gray: '#494949',
  },
});
